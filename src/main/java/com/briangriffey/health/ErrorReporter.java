package com.briangriffey.health;

/**
 *
 * Mock class that would do something with all of these errors that we're getting. Report them to some crash reporting
 * service or keep a moving average of errors. I'm sure that hipchat has some sort of error reporting service
 * so rather than going and implementing a whole thing I'll just leave this as a blank error reporter
 *
 */
public class ErrorReporter {

    public void reportError(Throwable e) {
        //do reporting here
    }
}
